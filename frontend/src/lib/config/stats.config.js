/**
 * Single source of truth for headline company stats shown across the site
 * (Hero, StatsStrip, About, EVPartnerSection). Update here to change everywhere.
 */
export const STATS = {
  installations: 500,
  evStations: 50,
  cities: 15,
  satisfaction: 98,
  yearsExperience: 10,
};
