// Re-exports from site.config.js — kept as a shim so existing
// imports (e.g. "@/lib/constants/navbar.constants") continue to work.
// New code should import from "@/lib/config/site.config" directly.

export { NAV_LINKS, NAV_CTA } from "@/lib/config/site.config";
